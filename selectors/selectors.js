const bannerSelector = $(`a[title="CYBER MONDAY EXTENDED"]`);
const bannerBtnCloseSelector = $(`button[class="close"][data-dismiss="modal"][aria-label="Close"]`);
const searchBarSelector = $(`input[type="search"][title="Search Site"]`);
const searchBtnSelector = $(`button*=Search`);
const itemsList = $(`div[class="item-cell"]`);
const tabSelector = $(`a[id="trendingBanner_720202"]`);

export { bannerSelector, bannerBtnCloseSelector, searchBarSelector, searchBtnSelector, itemsList, tabSelector };